# 0373 — Exception Hierarchy

Throw a specific error subtype and catch it through a base-type handler, printing `caught base`. In Groovy a `catch` clause for a supertype matches any subtype thrown.

## Run

    groovy main.groovy
