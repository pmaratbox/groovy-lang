# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. A ternary inside a closure evaluates the same path under both flag values.

## Run

    groovy main.groovy
