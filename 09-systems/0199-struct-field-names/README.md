# 0199 — Struct Field Names

List the field names of a Point{x,y} struct/record and print `x y`. Groovy 5 records expose `recordComponents` for declaration-order introspection.

## Run

    groovy main.groovy
