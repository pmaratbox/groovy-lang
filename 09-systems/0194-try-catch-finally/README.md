# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. Groovy uses Java's `try`/`catch`/`finally` directly.

## Run

    groovy main.groovy
