# 0200 — Precondition Assert

Check a precondition arg>0: for 5 print `ok`, and for -1 report the failure `error: must be positive`, on two lines. Groovy guards with a conditional that throws `IllegalArgumentException`, caught for the failure message.

## Run

    groovy main.groovy
