# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. Groovy enums expose `values()` and each constant's `name()`.

## Run

    groovy main.groovy
