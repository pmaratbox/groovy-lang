# 0388 — JSON Parse

Parse the JSON object `{"x":1,"y":2}` and print its entries `x=1 y=2`. Groovy collection methods split the flat object and rebuild ordered key=value pairs.

## Run

    groovy main.groovy
