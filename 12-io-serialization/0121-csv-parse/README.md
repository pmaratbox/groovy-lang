# 0121 — Parse CSV

Parse the two CSV rows `alice,30` and `bob,25` into name=value pairs and print `alice=30 bob=25`. Groovy's multiple assignment destructures each `split` result directly.

## Run

    groovy main.groovy
