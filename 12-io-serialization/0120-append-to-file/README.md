# 0120 — Append to a File

Write "a" to a file, append "b", then read both lines back and print `a b`. Groovy's `File.append` opens the file in append mode without manual stream handling.

## Run

    groovy main.groovy
