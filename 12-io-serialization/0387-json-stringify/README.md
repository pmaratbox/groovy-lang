# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. Groovy GString interpolation builds the compact string with no extra spaces.

## Run

    groovy main.groovy
