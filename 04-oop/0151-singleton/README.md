# 0151 — Singleton

Obtain a singleton instance twice and confirm both references are the same object, printing `same: yes`. The `@Singleton` transform supplies one shared `instance`.

## Run

    groovy main.groovy
