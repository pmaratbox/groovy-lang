# 0349 — Trait Composition

Compose two capabilities (A printing "a", B printing "b") into one type and invoke both, printing `a b`. Groovy `trait`s mix into a class via `implements`, contributing their methods.

## Run

    groovy main.groovy
