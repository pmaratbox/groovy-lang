# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. Overriding `toString` controls how `println` renders the object.

## Run

    groovy main.groovy
