# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. Groovy supports Java-style `default` methods directly in an `interface`.

## Run

    groovy main.groovy
