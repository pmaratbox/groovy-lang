# 0153 — Comparable Sort

Sort people [(alice,30),(bob,25)] by age ascending and print their names `bob alice`. Implementing `Comparable` with the `<=>` spaceship operator defines natural order.

## Run

    groovy main.groovy
