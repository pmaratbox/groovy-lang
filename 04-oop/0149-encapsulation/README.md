# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. A `private` field hides state, exposed only through methods.

## Run

    groovy main.groovy
