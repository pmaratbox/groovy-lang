# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. A `static` method returns a constructed instance, parsing hex pairs with Groovy range subscripts.

## Run

    groovy main.groovy
