# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. A `static` method is called on the class without an instance.

## Run

    groovy main.groovy
