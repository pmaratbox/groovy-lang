# 0130 — Format ISO Date

Format the date y=2026, m=6, d=4 as a zero-padded ISO string `2026-06-04`. Groovy's `sprintf` with `%02d` width specifiers handles the padding.

## Run

    groovy main.groovy
