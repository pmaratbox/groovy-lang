# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. Integer division via explicit `(int)` casts keeps the arithmetic exact.

## Run

    groovy main.groovy
