# 0063 — Flatten Nested List

Flatten the nested list `[[1, 2], [3, 4]]` into a single flat sequence and print its elements: `1 2 3 4`. `List.flatten()` flattens the nested list (it recurses through all levels, which here is just one).

## Run

    groovy main.groovy
