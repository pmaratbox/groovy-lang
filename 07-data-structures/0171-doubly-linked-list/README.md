# 0171 — Doubly Linked List

Build a doubly linked list 1<->2<->3, traverse forward then backward, printing `1 2 3` then `3 2 1` on two lines. Groovy lets us walk node references in a plain `for` loop and join with a space.

## Run

    groovy main.groovy
