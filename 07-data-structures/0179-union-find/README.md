# 0179 — Union-Find

Union (0,1) and (2,3), then query connectivity of (0,1)=yes and (0,2)=no, printing `yes no`. The disjoint-set uses path compression in a recursive Groovy `find`.

## Run

    groovy main.groovy
