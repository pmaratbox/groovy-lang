# 0173 — Trie

Insert "cat" and "car" into a trie, then search "car" (yes) and "can" (no), printing `yes no`. Groovy's `computeIfAbsent` on a map makes the child-node insertion concise.

## Run

    groovy main.groovy
