# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. A Groovy `List` backs the array heap with explicit sift-up and sift-down swaps.

## Run

    groovy main.groovy
