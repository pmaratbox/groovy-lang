# 0364 — Double-Ended Queue

Using a deque, push 1 and 2 to the back and 0 to the front, then print front-to-back `0 1 2`. Groovy reuses Java's `ArrayDeque` with `addFirst`/`addLast` for both ends.

## Run

    groovy main.groovy
