# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. Groovy adds `String.capitalize()`, which uppercases the first character of each split word.

## Run

    groovy main.groovy
