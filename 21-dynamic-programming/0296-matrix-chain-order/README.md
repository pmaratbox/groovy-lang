# 0296 — Matrix Chain Order

Find the minimum scalar multiplications to multiply matrices with dimensions [10,20,30,40], printing `18000`. Nested Groovy ranges explore every chain split point.

## Run

    groovy main.groovy
