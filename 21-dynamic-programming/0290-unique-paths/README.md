# 0290 — Unique Grid Paths

Count lattice paths from the top-left to bottom-right of a 3x3 grid moving right/down, printing `6`. Groovy `collect` builds the 2D DP grid of path counts.

## Run

    groovy main.groovy
