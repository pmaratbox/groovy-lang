# 0293 — Longest Palindromic Subsequence

Compute the longest palindromic subsequence length of "bbbab", printing `4`. Groovy string indexing `s[i]` reads characters inside the interval DP.

## Run

    groovy main.groovy
