# 0265 — Vending Machine

Insert coins 10, 10, 5 to reach the price 25 and dispense, printing `dispensed`. Groovy's `each` walks the coin stream while a guard tracks the accumulating total.

## Run

    groovy main.groovy
