# 0268 — Cellular Automaton Rule 90

Apply one Rule 90 step (new = left XOR right) to the row "00100" with zero boundaries, printing `01010`. Groovy's `^` XORs the neighbor bits inside a `collect`.

## Run

    groovy main.groovy
