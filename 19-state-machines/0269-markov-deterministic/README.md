# 0269 — Deterministic Transition Walk

Walk a deterministic transition map A->B->C->A for three steps from A, printing the visited states `B C A`. A Groovy map serves as the next-state lookup.

## Run

    groovy main.groovy
