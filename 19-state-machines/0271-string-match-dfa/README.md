# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. Groovy's `eachWithIndex` advances the DFA state over the text.

## Run

    groovy main.groovy
