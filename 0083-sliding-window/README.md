# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. `collate(2, 1, false)` makes windows of size 2 stepping by 1; the `false` drops the trailing incomplete window.

## Run

    groovy main.groovy
