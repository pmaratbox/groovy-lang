# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. `countBy` tallies each character and `max` picks the entry with the largest count.

## Run

    groovy main.groovy
