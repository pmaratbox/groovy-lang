# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. Groovy uses Java's `String.indexOf`, which returns the first match index or `-1`.

## Run

    groovy main.groovy
