# 0074 — Run-Length Encoding

Run-length encode the string `aaabbc` (each run of a repeated character becomes the character followed by its count), printing `a3b2c1`. `text[i]` indexes a one-character string; the inner loop counts the run and appends the character and count.

## Run

    groovy main.groovy
