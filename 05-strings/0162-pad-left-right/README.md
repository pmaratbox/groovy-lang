# 0162 — Pad Left and Right

Left-pad and right-pad "5" with spaces to width 3 and print both joined by a pipe: `  5|5  `. Groovy's `padLeft` and `padRight` fill with spaces to the target width.

## Run

    groovy main.groovy
