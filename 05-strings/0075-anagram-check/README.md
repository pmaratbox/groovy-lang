# 0075 — Anagram Check

Check whether each pair is an anagram by comparing sorted letters — `listen`/`silent` and `hello`/`world` — printing `listen/silent: yes` and `hello/world: no`. `toList().sort()` turns each word into a sorted list of characters; equal sorted lists mean the words are anagrams.

## Run

    groovy main.groovy
