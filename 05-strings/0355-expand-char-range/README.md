# 0355 — Expand Character Range

Expand the range spec "a-e" into the characters `abcde`. Groovy ranges work directly on single-character strings, so `(start..end)` enumerates them.

## Run

    groovy main.groovy
