# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. A `Set.add` returns false on repeats, so `findAll` keeps only first-seen characters in order.

## Run

    groovy main.groovy
