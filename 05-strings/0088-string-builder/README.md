# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. Groovy uses Java's `StringBuilder`; `append` chains pieces into the mutable buffer.

## Run

    groovy main.groovy
