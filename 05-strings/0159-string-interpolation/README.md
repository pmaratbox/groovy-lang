# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. Groovy GStrings interpolate `$name` and `$age` directly inside double quotes.

## Run

    groovy main.groovy
