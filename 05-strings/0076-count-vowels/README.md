# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `String.each` iterates the characters (as one-character strings); `'aeiou'.contains(ch)` tests each for a vowel.

## Run

    groovy main.groovy
