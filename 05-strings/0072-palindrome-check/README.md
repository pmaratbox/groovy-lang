# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. Groovy's `String.reverse()` gives the mirror image directly, which is compared to the original.

## Run

    groovy main.groovy
