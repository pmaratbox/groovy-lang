# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. b is a rotation of a iff they share a length and b is a substring of a+a, checked with `contains`.

## Run

    groovy main.groovy
