# 0415 — SwitchMap

Implement switchMap: when a new outer value arrives, cancel the previous inner subscription before starting the new one. Groovy closures and maps model observers, while a virtual-time scheduler keeps timed cancellation deterministic.

## Run

    groovy main.groovy
