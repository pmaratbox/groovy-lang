# 0410 — Merge Streams

Implement merge of two timed streams using a virtual-time scheduler, interleaving them by emission time. Groovy closures stored in a map make a lightweight observer with `next`/`error`/`complete`.

## Run

    groovy main.groovy
