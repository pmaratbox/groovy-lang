# 0401 — Create an Observable

Build a push-based Observable from scratch that emits 1, 2, 3 to its observer and then completes. In Groovy the Observable is just a closure taking an observer map of `next`/`complete` closures.

## Run

    groovy main.groovy
