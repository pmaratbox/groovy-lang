# 0417 — BehaviorSubject

Implement a BehaviorSubject that holds a current value and replays it immediately to each new subscriber. Groovy closures make each observer a concise `next` handler stored in a list.

## Run

    groovy main.groovy
