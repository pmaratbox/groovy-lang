# 0411 — Concat Streams

Implement concat: subscribe to the second source only after the first completes; concat [1,2] then [3,4]. Groovy closures stored in a Map model the observer's next/error/complete callbacks.

## Run

    groovy main.groovy
