# 0406 — Filter Operator

Implement a filter operator that forwards only values passing a predicate, keeping the even numbers of 1..6. Groovy closures make the predicate and observer callbacks concise.

## Run

    groovy main.groovy
