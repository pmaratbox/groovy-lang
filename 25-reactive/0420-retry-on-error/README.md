# 0420 — Retry On Error

Implement retry(n) that resubscribes to the source on error up to n times; the source succeeds on the 3rd subscription. Groovy closures captured in a map form the observer, and a recursive closure drives the resubscription.

## Run

    groovy main.groovy
