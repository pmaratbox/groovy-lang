# 0412 — Combine Latest

Implement combineLatest of two timed streams, emitting the pair of latest values whenever either source emits (once both have emitted). Groovy maps double as ad-hoc observers, with GString interpolation formatting each emitted pair.

## Run

    groovy main.groovy
