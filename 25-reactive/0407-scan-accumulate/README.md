# 0407 — Scan (Running Fold)

Implement a scan operator that emits the running accumulation; produce the running sums of 1, 2, 3, 4. In Groovy the observer is a Map of closures and scan closes over a running state variable.

## Run

    groovy main.groovy
