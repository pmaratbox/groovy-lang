# 0008 — Strings

Given `name = "world"`, print a greeting, the name in uppercase, and its
length. Double-quoted strings are `GString`s and interpolate with `${...}`
(single quotes would be a plain `String`, no interpolation). `.toUpperCase()`
and `.length()` come straight from Java's `String`.

## Run

    groovy main.groovy
