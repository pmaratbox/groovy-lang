# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. A class implementing `Iterator` exposes `hasNext()`/`next()` over the range.

## Run

    groovy main.groovy
