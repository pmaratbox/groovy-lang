# 0218 — Flyweight

Share flyweight instances for repeated characters a, b, a and print the number of distinct objects created `2`. A factory caches glyphs with `computeIfAbsent`, creating each key only once.

## Run

    groovy main.groovy
