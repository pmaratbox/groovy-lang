# 0204 — Adapter

Adapt a Celsius source (100) to a Fahrenheit interface, printing `212`. The adapter wraps the Celsius object and implements `FahrenheitReading` with the conversion formula.

## Run

    groovy main.groovy
