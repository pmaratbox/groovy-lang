# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. A `ThemeFactory` interface yields a matching family of dark widgets.

## Run

    groovy main.groovy
