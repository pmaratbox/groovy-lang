# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. Interchangeable closures stored in a map serve as pluggable strategies.

## Run

    groovy main.groovy
