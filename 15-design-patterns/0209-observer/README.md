# 0209 — Observer

Notify two observers of a new value 5; each prints its id and the value on its own line. The subject keeps a list of observers and dispatches the value via `each`.

## Run

    groovy main.groovy
