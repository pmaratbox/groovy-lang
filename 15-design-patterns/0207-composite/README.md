# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. A `Composite` sums its children via Groovy's `sum` closure, while a `Leaf` returns its value.

## Run

    groovy main.groovy
