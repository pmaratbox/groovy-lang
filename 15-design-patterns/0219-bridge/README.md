# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The `Shape` abstraction holds a `Color` implementor, decoupling shape from color.

## Run

    groovy main.groovy
