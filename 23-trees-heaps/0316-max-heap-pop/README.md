# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. A `PriorityQueue` with `Collections.reverseOrder()` turns the JDK min-heap into a max-heap in Groovy.

## Run

    groovy main.groovy
