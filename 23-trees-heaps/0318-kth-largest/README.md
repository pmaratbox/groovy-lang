# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. A bounded `PriorityQueue` (a min-heap of size k) keeps only the largest seen in Groovy.

## Run

    groovy main.groovy
