# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. An iterative array-backed segment tree class keeps the range query compact in Groovy.

## Run

    groovy main.groovy
