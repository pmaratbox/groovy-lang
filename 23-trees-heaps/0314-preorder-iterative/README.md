# 0314 — Iterative Preorder

Print the iterative (stack-based) preorder traversal of the BST built from 5,3,8,1,4 `5 3 1 4 8`. An `ArrayDeque` used as a stack pushes right before left for the correct order in Groovy.

## Run

    groovy main.groovy
