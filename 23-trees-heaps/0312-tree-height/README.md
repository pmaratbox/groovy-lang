# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. A single ternary expression with `Math.max` captures the recursive height idiomatically in Groovy.

## Run

    groovy main.groovy
