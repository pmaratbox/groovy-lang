# 0313 — Level-Order Traversal

Print the breadth-first (level-order) traversal of the BST built from 5,3,8,1,4 `5 3 8 1 4`. A `LinkedList` used as a FIFO queue gives a clean BFS in Groovy.

## Run

    groovy main.groovy
