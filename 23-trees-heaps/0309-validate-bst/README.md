# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. A nullable `Long` bound makes the recursive range check idiomatic in Groovy.

## Run

    groovy main.groovy
