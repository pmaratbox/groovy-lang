# 0315 — Tree Diameter

Compute the diameter (longest path in edges) of a tree where root has children A and B, and A has children C and D, printing `3`. A height recursion that updates a captured `best` field tracks the diameter idiomatically in Groovy.

## Run

    groovy main.groovy
