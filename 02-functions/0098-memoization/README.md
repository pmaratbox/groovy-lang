# 0098 — Memoization

Compute `fibonacci(10)` recursively with memoization (caching each result so it is computed once) and print it: `55`. A closure assigned to `fib` (declared first) can call itself recursively while a map caches each result.

## Run

    groovy main.groovy
