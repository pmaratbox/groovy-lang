# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. Groovy's `join` turns the collected list into one space-separated line.

## Run

    groovy main.groovy
