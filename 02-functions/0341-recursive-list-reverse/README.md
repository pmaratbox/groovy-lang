# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. Groovy builds the reversed list by appending the head after recursing on the tail.

## Run

    groovy main.groovy
