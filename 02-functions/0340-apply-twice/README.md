# 0340 — Apply Twice

Write applyTwice(f,x)=f(f(x)) and apply it with inc to 3, printing `5`. Groovy closures pass as first-class arguments to the higher-order function.

## Run

    groovy main.groovy
