# 0329 — CPS Factorial

Compute 5! in continuation-passing style, printing `120`. Each recursive step receives a continuation closure, and the outermost continuation simply prints the final result.

## Run

    groovy main.groovy
