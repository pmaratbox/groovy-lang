# 0325 — Point-Free Style

Express "sum of squares" point-free (compose map-square with sum) and apply it to [1,2,3], printing `14`. Groovy's `>>` operator composes closures left-to-right so the pipeline never names its argument.

## Run

    groovy main.groovy
