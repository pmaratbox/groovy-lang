# 0304 — Connected Components

Count the connected components of the undirected graph with edges (0,1),(1,2),(3,4) over nodes 0..4, printing `2`. Groovy uses a list as a stack via `<<` and `pop` for the iterative DFS.

## Run

    groovy main.groovy
