# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. Range subscripts take the tail `[k..<size]` and head `[0..<k]`; concatenating them rotates left.

## Run

    groovy main.groovy
