# 0080 — Deduplicate

Remove duplicates from `1, 2, 2, 3, 1`, keeping the first occurrence of each in order, and print `1 2 3`. `unique(false)` returns a new list with duplicates removed in first-seen order (the `false` means do not mutate the original).

## Run

    groovy main.groovy
