# 0168 — List Difference

Compute [1,2,3,4] minus the elements of [2,4], printing `1 3`. Groovy's `-` operator on lists removes matching elements while preserving order.

## Run

    groovy main.groovy
