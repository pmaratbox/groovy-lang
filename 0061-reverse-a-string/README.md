# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. Groovy adds a `reverse()` method to `String` that returns the characters in reverse order.

## Run

    groovy main.groovy
