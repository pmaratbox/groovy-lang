# 0062 — Do-While Loop

Use a do-while loop (whose body runs once before the condition is checked) to print `1`, `2`, and `3` on one line: `1 2 3`. Groovy supports `do { ... } while (cond)` (since Groovy 3.0); the body runs once before the first test.

## Run

    groovy main.groovy
