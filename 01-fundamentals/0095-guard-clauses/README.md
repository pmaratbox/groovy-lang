# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. Each guard returns as soon as its condition holds, avoiding nested branches.

## Run

    groovy main.groovy
