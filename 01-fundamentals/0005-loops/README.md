# 0005 — Loops

Print 1..5 with a for-in loop over an inclusive range. Groovy's `1..5` is
an **inclusive** `Range` (use `1..<5` for half-open). The more idiomatic
functional style is `(1..5).each { println it }`.

## Run

    groovy main.groovy
