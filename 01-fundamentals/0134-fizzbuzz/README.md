# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. Groovy's `(1..15).each` ranges read like the spec.

## Run

    groovy main.groovy
