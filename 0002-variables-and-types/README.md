# 0002 — Variables & basic types

Declare statically-typed bindings (`int`, `double`, `String`, `boolean`) and
print them. Groovy also supports dynamic `def` declarations; the static form
is shown here to make the types obvious. `$var` inside double-quoted strings
is GString interpolation.

## Run

    groovy main.groovy
