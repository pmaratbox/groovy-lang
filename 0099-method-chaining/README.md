# 0099 — Method Chaining

Build a calculator that supports method chaining — start at `5`, then `.add(3).multiply(2)` — and print the result: `16`. Each mutating method returns `this`, so the calls chain fluently.

## Run

    groovy main.groovy
