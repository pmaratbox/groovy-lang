# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A Groovy Closure captures the mutable counter from its enclosing scope.

## Run

    groovy main.groovy
