# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. A Groovy `record` with a custom `toString` yields the exact structured form.

## Run

    groovy main.groovy
