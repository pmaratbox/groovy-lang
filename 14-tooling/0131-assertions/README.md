# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. A tiny closure helper throws an `AssertionError` on failure, idiomatic for ad-hoc Groovy checks.

## Run

    groovy main.groovy
