# 0400 — ASCII Histogram

Render a histogram of [3,1,2] as rows of "#" characters, one bar per line. Groovy's String multiply operator renders each bar in a single expression.

## Run

    groovy main.groovy
