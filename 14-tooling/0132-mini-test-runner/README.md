# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. Each test is a closure run inside a `try/catch` so a thrown `AssertionError` tallies as a failure.

## Run

    groovy main.groovy
