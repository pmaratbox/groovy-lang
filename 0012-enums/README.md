# 0012 — Enums

Define a `Color` enum (`RED`, `GREEN`, `BLUE`) and print the position of `GREEN`
(1) and `BLUE` (2). Groovy uses Java enums: each member is a singleton object,
and `.ordinal()` returns its zero-based declaration position.

## Run

    groovy main.groovy
