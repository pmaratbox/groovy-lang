# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. Groovy's `groupBy` keys each word by its sorted letters in one line.

## Run

    groovy main.groovy
