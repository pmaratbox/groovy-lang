# 0279 — Minimum Window Substring

Find the smallest window in "ADOBECODEBANC" containing all of "ABC", printing `BANC`. Groovy's `withDefault` maps make the sliding-window counters concise.

## Run

    groovy main.groovy
