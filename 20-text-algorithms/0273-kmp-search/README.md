# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. Groovy's int[] array literals build the prefix-function compactly.

## Run

    groovy main.groovy
