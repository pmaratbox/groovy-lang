# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Groovy's range `.sort` with a comparator orders indices by their suffix text.

## Run

    groovy main.groovy
