# 0283 — Rabin-Karp Search

Use rolling-hash Rabin-Karp to find all start indices of "ab" in "xabxab", printing `1 4`. Groovy's `long` literals with underscores keep the modulus readable.

## Run

    groovy main.groovy
