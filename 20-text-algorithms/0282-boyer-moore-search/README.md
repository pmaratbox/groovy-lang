# 0282 — Boyer-Moore Search

Use the bad-character rule to find "abc" in "zzabc", printing the index `2`. A Groovy map holds the last-occurrence table for the bad-character shift.

## Run

    groovy main.groovy
