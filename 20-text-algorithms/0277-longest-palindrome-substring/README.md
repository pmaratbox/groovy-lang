# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. A small Groovy closure expands around each center and returns the bounds as a list.

## Run

    groovy main.groovy
