# 0431 — Concat Streams

Use the library's concat operator so the second stream starts only after the first completes. Uses RxJava 3's `Observable.concat` to join two synchronous streams in order.

## Run

    groovy main.groovy
