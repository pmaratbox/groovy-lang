# 0427 — Filter Operator

Use the library's filter operator to keep the even values of 1..6. Uses RxJava 3 (via Grape) with `Observable.fromArray(...).filter { ... }`.

## Run

    groovy main.groovy
