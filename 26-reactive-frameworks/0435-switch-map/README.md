# 0435 — SwitchMap

Use the library's switchMap on a virtual/test scheduler so a new outer value cancels the previous inner stream. Built with RxJava 3's Observable.switchMap driven by a TestScheduler.

## Run

    groovy main.groovy
