# 0425 — Create a Stream

Create a stream of 1, 2, 3 with your language's reactive library and print each value, then print done on completion. Uses RxJava 3's `Observable.fromArray` creation operator and prints "done" from the onComplete callback.

## Run

    groovy main.groovy
