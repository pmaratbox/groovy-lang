# 0426 — Map Operator

Use the library's map operator to double each value of a 1..4 stream. Built with RxJava 3 (via Grape), using Observable.fromArray and the map operator.

## Run

    groovy main.groovy
