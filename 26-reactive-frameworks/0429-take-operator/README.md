# 0429 — Take Operator

Use the library's take operator to take the first 3 values of an unbounded stream, then complete. Uses RxJava 3 with Observable.generate as the unbounded source and the take operator to bound it.

## Run

    groovy main.groovy
