# 0084 — Partition

Partition `1, 2, 3, 4, 5, 6` into evens and odds, printing `evens: 2 4 6` and `odds: 1 3 5`. `List.split { ... }` returns a two-element list: the items that match the closure, then those that do not.

## Run

    groovy main.groovy
