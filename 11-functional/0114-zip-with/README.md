# 0114 — Zip With

Combine [1,2,3] and [4,5,6] element-wise with addition, printing `5 7 9`. Groovy's `.transpose()` pairs the lists up, and `.collect` adds each pair.

## Run

    groovy main.groovy
