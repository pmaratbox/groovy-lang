# 0115 — Dispatch Table

Store functions in a map keyed by name, then apply "add" and "mul" to (3,4), printing `7 12`. A Groovy map can hold closures as values, and `ops.add(3, 4)` looks one up and calls it.

## Run

    groovy main.groovy
