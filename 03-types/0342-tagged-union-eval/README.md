# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. Groovy uses an abstract base with subclasses and a `switch` on type for the recursive eval.

## Run

    groovy main.groovy
