# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. Groovy has no dedicated alias keyword, so an `import ... as Meters` gives a type a semantic name.

## Run

    groovy main.groovy
