# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Groovy classes nest naturally and `person.address.city` walks the chain of auto-generated getters.

## Run

    groovy main.groovy
