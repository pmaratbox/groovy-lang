# 0146 — Numeric Conversions

Truncate 3.9 to the int 3 and widen the int 3 to the float 3.0, printing `3 3.0`. Groovy's `(int)` cast truncates toward zero and `(float)` widens, with a float printing as `3.0`.

## Run

    groovy main.groovy
