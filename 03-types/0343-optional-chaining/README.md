# 0343 — Optional Chaining

Read a deeply optional value a.b.c when present (5) and when absent (default 0), printing `5 0`. Groovy's safe-navigation `?.` short-circuits a missing link and the Elvis `?:` supplies the fallback.

## Run

    groovy main.groovy
