# 0144 — Flag Enum (Bit Flags)

Combine bit flags READ(1) and WRITE(2) into 3, check that WRITE is set, and print `3 yes`. Groovy uses Java's bitwise `|` and `&` operators on plain ints for flag sets.

## Run

    groovy main.groovy
