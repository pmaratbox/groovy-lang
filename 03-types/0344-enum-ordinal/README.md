# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. Groovy enums are Java enums, so `.ordinal()` and `values()[i].name()` read the declaration order.

## Run

    groovy main.groovy
