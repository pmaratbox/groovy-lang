# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. Groovy's `==` calls `equals`, which compares lists element-by-element recursively.

## Run

    groovy main.groovy
