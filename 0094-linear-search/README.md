# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. `findIndexOf` returns the index of the first element matching the closure (or `-1`), which is exactly a linear search.

## Run

    groovy main.groovy
