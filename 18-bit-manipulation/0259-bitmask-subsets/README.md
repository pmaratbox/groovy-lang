# 0259 — Enumerate Submasks

Enumerate all submasks of the mask 5 (101) in descending order `5 4 1 0`. The classic `(sub-1) & mask` loop collects into a Groovy list joined for output.

## Run

    groovy main.groovy
