# 0252 — Reverse Bits in a Byte

Reverse the 8 bits of the value 1 (00000001) to get 128 (10000000), printing `128`. A Groovy range `(0..<8).each` drives the bit-by-bit reversal.

## Run

    groovy main.groovy
