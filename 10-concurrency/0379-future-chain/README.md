# 0379 — Future Chain

Chain asynchronous steps 5 -> (*2) -> (+1) and print the final value `11`. Groovy composes `java.util.concurrent.CompletableFuture` stages with `thenApply`.

## Run

    groovy main.groovy
