# 0055 — Frequency Count

Count how many times each letter appears in `banana` and print the per-letter counts in alphabetical order: `a:3 b:1 n:2`. `countBy { it }` builds the letter-to-count map in one call; its key set is then sorted for alphabetical output.

## Run

    groovy main.groovy
