# 0238 — Fraction Addition

Add the fractions 1/2 and 1/3 and print the reduced result `5/6`. Groovy uses multiple assignment in a `gcd` helper and a GString to format the reduced fraction.

## Run

    groovy main.groovy
