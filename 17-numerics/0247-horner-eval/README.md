# 0247 — Horner Polynomial Eval

Evaluate 2x^2+3x+1 at x=2 using the Horner method, printing `15`. Groovy folds the coefficient list with `inject` to apply `acc*x + c`.

## Run

    groovy main.groovy
