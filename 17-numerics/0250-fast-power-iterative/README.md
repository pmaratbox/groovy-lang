# 0250 — Iterative Fast Power

Compute 2^10 by iterative binary exponentiation, printing `1024`. Groovy squares the base while right-shifting the exponent, multiplying in on each set low bit.

## Run

    groovy main.groovy
