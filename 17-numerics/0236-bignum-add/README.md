# 0236 — Big Number Addition

Add the large numbers 999999999999 and 1 as digit strings, printing `1000000000000`. Groovy uses a StringBuilder and `Character.getNumericValue` to add digit-by-digit with carry.

## Run

    groovy main.groovy
