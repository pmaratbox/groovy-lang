# 0071 — Celsius to Fahrenheit

Convert `100` and `0` degrees Celsius to Fahrenheit using `F = C * 9 / 5 + 32`, printing `100C = 212F` and `0C = 32F`. Groovy `/` yields a `BigDecimal`, but assigning to an `int f` coerces it back to a whole number.

## Run

    groovy main.groovy
