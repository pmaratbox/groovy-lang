# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. Groovy's replaceAll accepts a closure that receives each regex match group.

## Run

    groovy main.groovy
