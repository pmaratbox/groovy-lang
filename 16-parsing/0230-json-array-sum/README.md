# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. Groovy's collect maps the split tokens to ints, then sum folds them.

## Run

    groovy main.groovy
