# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. Groovy's split with a regex separates words on whitespace runs.

## Run

    groovy main.groovy
