# 0222 — Bracket Matching

Check that mixed brackets are balanced: "([{}])" (yes) and "([)]" (no), printing `yes no`. Groovy uses a List as a stack via the leftShift and pop methods.

## Run

    groovy main.groovy
