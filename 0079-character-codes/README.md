# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. `'A' as char` makes a `char`, cast to `int` for its code; `(char) 66` casts the code back to a character.

## Run

    groovy main.groovy
