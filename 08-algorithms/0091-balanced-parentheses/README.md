# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. A list is used as the stack (`push`/`pop`); an empty stack at the end means the brackets balanced.

## Run

    groovy main.groovy
