# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. A map records each value's index; the needed complement is looked up before storing the current value, giving a single-pass solution.

## Run

    groovy main.groovy
