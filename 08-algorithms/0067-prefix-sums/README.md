# 0067 — Prefix Sums

Compute the running totals (prefix sums) of `1, 2, 3, 4` — each element added to the sum of all the previous ones — and print them: `1 3 6 10`. `collect` with a running `total` (`total += it`) produces the cumulative sums.

## Run

    groovy main.groovy
