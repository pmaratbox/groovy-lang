# 0367 — Selection Sort

Selection-sort [5,1,4,2] ascending, printing `1 2 4 5`. Repeatedly select the minimum of the rest and swap it into place using a classic indexed loop.

## Run

    groovy main.groovy
